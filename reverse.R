#Create a numeric vector with elements from 1 to 10
vector<-10:1

#Reverse the vector
reversed_vector<-rev(vector)

#Print the reversed vector
cat("Elements in Reverse order:", reversed_vector,"\n")