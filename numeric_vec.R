#Create a numeric vector with elements from 1 to 25
vec<-1:25

#Initialize an empty vector to store even numbers
even_number<-c()

#Iterate through the vector and store elements that are even numbers
for(num in vec){
}